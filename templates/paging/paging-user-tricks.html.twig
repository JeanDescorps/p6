<div class="row justify-content-center">
    {% set page = paging.currentPage %}
    {% if paging.pages == 0 %}
        {% set pages = 1 %}
    {% else %}
        {% set pages = paging.pages %}
    {% endif %}
    <div class="mb-3">
        <ul class="pagination justify-content-center">
            {% if page > 1 %}
                <li class="page-item">
                    <a class="page-link" href="{{ path(route, {'id' : user.id, 'page' : pageC, 'pageT' : 1 })}}#paging-tricks"><<</a>
                <li>
                <li class="page_item">
                    <a class="page-link" href="{{ path(route, {'id' : user.id, 'page' : pageC, 'pageT' : page - 1 })}}#paging-tricks">Page précédente</a>
                </li>
            {% endif %}
            {% for i in 1..pages %}
                <li class="page-item {% if page == i %}active{% endif %}">
                    <a class="page-link" href="{{ path(route, {'id' : user.id, 'page' : pageC, 'pageT' : i })}}#paging-tricks">{{ i }}</a>
                </li>
            {% endfor %}
            {% if page < pages %}
            <li class="page-item">
               <a class="page-link" href="{{ path(route, {'id' : user.id, 'page' : pageC, 'pageT' : page + 1 })}}#paging-tricks">Page suivante</a>
            </li>
            <li class="page-item">
                <a class="page-link" href="{{ path(route, {'id' : user.id, 'page' : pageC, 'pageT' : pages })}}#paging-tricks">>></a> 
            </li>
            {% endif %}
        </ul>
    </div>
</div>
